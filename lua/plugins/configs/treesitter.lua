local options = {
  ensure_installed = { "lua", "rust", "c++", "html", "css", "javascript", "nu" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
